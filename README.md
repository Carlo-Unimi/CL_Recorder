 # CL Recorder
 ---

this is a simple Linux command-line interface tool that utilizes the ALSA library for audio recording. 
it supports concurrent recording of up to **32 channels**, with output options including the **WAV** format or direct encoding to **MP3** (*standard quality*).
